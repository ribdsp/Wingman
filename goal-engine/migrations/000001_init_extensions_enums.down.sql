DROP FUNCTION IF EXISTS set_updated_at();

DROP TYPE IF EXISTS audit_actor_type;
DROP TYPE IF EXISTS approval_resolution;
DROP TYPE IF EXISTS approval_outcome;
DROP TYPE IF EXISTS dispatch_status;
DROP TYPE IF EXISTS evaluation_decision;
DROP TYPE IF EXISTS goal_comparator;
DROP TYPE IF EXISTS goal_status;
